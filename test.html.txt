<tool_use>
{"name":"create_code","input":{"action_description":"Building shop management system...","features_json":"{\n  "persistence_enabled": true,\n  "app_name": "Auto Wrap Pro",\n  "avoids_browser_dialogs": true,\n  "avoids_viewport_units": true,\n  "data_paradigm": "B",\n  "verified_data_sdk_usage": true,\n  "verified_editable_features_usage": true,\n  "schema": {\n    "type": "string",\n    "name": "string",\n    "email": "string",\n    "phone": "string",\n    "cpf": "string",\n    "vehicle": "string",\n    "plate": "string",\n    "vehicle_color": "string",\n    "vehicle_year": "string",\n    "service_name": "string",\n    "service_category": "string",\n    "service_price": "string",\n    "material_name": "string",\n    "material_brand": "string",\n    "material_qty": "string",\n    "material_unit": "string",\n    "collab_name": "string",\n    "collab_role": "string",\n    "collab_pin": "string",\n    "order_client_name": "string",\n    "order_vehicle": "string",\n    "order_plate": "string",\n    "order_services": "string",\n    "order_total": "string",\n    "order_status": "string",\n    "order_operator": "string",\n    "order_date": "string",\n    "order_due_date": "string",\n    "order_notes": "string",\n    "order_checklist": "string",\n    "order_is_quote": "string",\n    "created_at": "string"\n  },\n  "edit_panel": {\n    "brand_label": {\n      "type": "label",\n      "text": "Identidade da Marca",\n      "size": "large"\n    },\n    "app_title": {\n      "type": "text_input",\n      "label": "Nome do App",\n      "placeholder": "Ex: Auto Wrap Pro"\n    },\n    "company_name": {\n      "type": "text_input",\n      "label": "Nome da Empresa",\n      "placeholder": "Ex: Minha Empresa"\n    },\n    "welcome_text": {\n      "type": "text_input",\n      "label": "Texto de Boas-vindas",\n      "placeholder": "Bem-vindo ao sistema"\n    }\n  }\n}","html_content":"\n<html lang="pt-BR" class="h-full">\n\n<meta charset="UTF-8">\n<meta name="viewport" content="width=device-width, initial-scale=1.0">\n<meta name="apple-mobile-web-app-capable" content="yes">\n<meta name="mobile-web-app-capable" content="yes">\nAuto Wrap Pro\n<script src="https://cdn.tailwindcss.com/3.4.17">\n<script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js">\n<script src="/_sdk/element_sdk.js">\n<script src="/_sdk/data_sdk.js">\n<link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">\n\ntailwind.config = {\n  theme: {\n    extend: {\n      fontFamily: { jakarta: ['Plus Jakarta Sans', 'sans-serif'] },\n      colors: {\n        brand: { 50:'#f0fdf4',100:'#dcfce7',200:'#bbf7d0',300:'#86efac',400:'#4ade80',500:'#22c55e',600:'#16a34a',700:'#15803d',800:'#166534',900:'#14532d' },\n        surface: { 50:'#fafbfc',100:'#f4f6f8',200:'#e9ecf0',300:'#d5dbe3' },\n        ink: { 50:'#f8fafc',100:'#94a3b8',200:'#64748b',300:'#475569',400:'#334155',500:'#1e293b',600:'#0f172a' }\n      }\n    }\n  }\n}\n\n\n  * { font-family: 'Plus Jakarta Sans', sans-serif; }\n  html, body { height: 100%; margin: 0; }\n  .app-root { height: 100%; overflow: auto; }\n  .fade-in { animation: fadeIn 0.3s ease-out; }\n  @keyframes fadeIn { from { opacity:0; transform:translateY(8px); } to { opacity:1; transform:translateY(0); } }\n  .slide-in { animation: slideIn 0.3s ease-out; }\n  @keyframes slideIn { from { opacity:0; transform:translateX(-12px); } to { opacity:1; transform:translateX(0); } }\n  .card-hover { transition: all 0.2s ease; }\n  .card-hover:hover { transform: translateY(-2px); box-shadow: 0 8px 25px rgba(0,0,0,0.08); }\n  .tv-pulse { animation: tvPulse 2s ease-in-out infinite; }\n  @keyframes tvPulse { 0%,100% { opacity:1; } 50% { opacity:0.7; } }\n  .status-bar { animation: statusSlide 20s linear infinite; }\n  @keyframes statusSlide { 0% { transform: translateX(0); } 100% { transform: translateX(-50%); } }\n  .toast-anim { animation: toastIn 0.3s ease-out; }\n  @keyframes toastIn { from { opacity:0; transform:translateY(20px); } to { opacity:1; transform:translateY(0); } }\n  input, select, textarea { font-family: 'Plus Jakarta Sans', sans-serif; }\n  ::-webkit-scrollbar { width: 6px; }\n  ::-webkit-scrollbar-track { background: transparent; }\n  ::-webkit-scrollbar-thumb { background: #d5dbe3; border-radius: 3px; }\n  .modal-overlay { background: rgba(15,23,42,0.4); backdrop-filter: blur(4px); }\n  .btn-primary { background: #22c55e; color: white; transition: all 0.2s; }\n  .btn-primary:hover { background: #16a34a; }\n  .btn-secondary { background: #e9ecf0; color: #334155; transition: all 0.2s; }\n  .btn-secondary:hover { background: #d5dbe3; }\n  .btn-danger { background: #fee2e2; color: #dc2626; transition: all 0.2s; }\n  .btn-danger:hover { background: #fecaca; }\n  .input-field { border: 1.5px solid #e9ecf0; border-radius: 10px; padding: 10px 14px; width: 100%; transition: all 0.2s; background: white; font-size: 14px; }\n  .input-field:focus { outline: none; border-color: #22c55e; box-shadow: 0 0 0 3px rgba(34,197,94,0.1); }\n  .sidebar-item { transition: all 0.15s; border-radius: 10px; padding: 10px 14px; cursor: pointer; display: flex; align-items: center; gap: 10px; color: #64748b; font-weight: 500; font-size: 14px; }\n  .sidebar-item:hover { background: #f0fdf4; color: #16a34a; }\n  .sidebar-item.active { background: #f0fdf4; color: #16a34a; font-weight: 600; }\n  .badge { display: inline-flex; align-items: center; padding: 3px 10px; border-radius: 20px; font-size: 12px; font-weight: 600; }\n  .badge-green { background: #dcfce7; color: #16a34a; }\n  .badge-yellow { background: #fef9c3; color: #a16207; }\n  .badge-blue { background: #dbeafe; color: #2563eb; }\n  .badge-red { background: #fee2e2; color: #dc2626; }\n  .badge-gray { background: #f1f5f9; color: #64748b; }\n  .badge-purple { background: #f3e8ff; color: #9333ea; }\n  .table-row { transition: background 0.15s; }\n  .table-row:hover { background: #f8fafc; }\n  @media (max-width: 768px) {\n    .mobile-sidebar { position: fixed; left: -280px; top: 0; height: 100%; width: 260px; z-index: 50; transition: left 0.3s ease; background: white; }\n    .mobile-sidebar.open { left: 0; }\n  }\n\n\n<body class="h-full bg-surface-50">\n<div class="app-root" id="appRoot">\n\n  \n  <div id="toastContainer" class="fixed top-4 right-4 z-[100] flex flex-col gap-2">\n\n  \n  <div id="loginScreen" class="h-full flex items-center justify-center p-4" style="background: linear-gradient(135deg, #f0fdf4 0%, #fafbfc 50%, #dcfce7 100%);">\n    <div class="bg-white rounded-2xl shadow-xl p-8 w-full max-w-sm fade-in">\n      <div class="text-center mb-8">\n        <div class="w-16 h-16 bg-brand-500 rounded-2xl flex items-center justify-center mx-auto mb-4 shadow-lg shadow-brand-200">\n          <i data-lucide="car" class="text-white" style="width:32px;height:32px;">\n        \n        <h1 id="loginTitle" class="text-2xl font-800 text-ink-600">Auto Wrap Pro\n        <p id="loginWelcome" class="text-ink-200 text-sm mt-1">Bem-vindo ao sistema de gestão\n      \n      <div class="space-y-3">\n        <button onclick="enterAs('gestor')" class="w-full btn-primary rounded-xl py-3 font-600 text-sm flex items-center justify-center gap-2">\n          <i data-lucide="shield" style="width:18px;height:18px;"> Entrar como Gestor\n        \n        <button onclick="enterAs('operador')" class="w-full btn-secondary rounded-xl py-3 font-600 text-sm flex items-center justify-center gap-2">\n          <i data-lucide="wrench" style="width:18px;height:18px;"> Entrar como Operador\n        \n        <button onclick="enterAs('tv')" class="w-full bg-ink-600 text-white rounded-xl py-3 font-600 text-sm flex items-center justify-center gap-2 hover:bg-ink-500 transition">\n          <i data-lucide="monitor" style="width:18px;height:18px;"> Modo TV / Monitor\n        \n      \n    \n  \n\n  \n  <div id="gestorPanel" class="h-full hidden">\n    <div class="flex h-full">\n      \n      <aside id="gestorSidebar" class="w-64 bg-white border-r border-surface-200 flex flex-col mobile-sidebar">\n        <div class="p-5 border-b border-surface-200">\n          <div class="flex items-center gap-3">\n            <div class="w-10 h-10 bg-brand-500 rounded-xl flex items-center justify-center shadow-sm shadow-brand-200">\n              <i data-lucide="car" class="text-white" style="width:20px;height:20px;">\n            \n            \n              <h2 id="sidebarTitle" class="font-700 text-ink-600 text-sm">Auto Wrap Pro\n              <p class="text-xs text-ink-100">Painel do Gestor\n            \n          \n        \n        <nav class="flex-1 p-3 space-y-1 overflow-auto" id="gestorNav">\n        <div class="p-3 border-t border-surface-200">\n          <button onclick="logout()" class="sidebar-item w-full text-red-500 hover:bg-red-50">\n            <i data-lucide="log-out" style="width:18px;height:18px;"> Sair\n          \n        \n      \n      \n      <main class="flex-1 flex flex-col overflow-hidden">\n        <header class="bg-white border-b border-surface-200 px-6 py-4 flex items-center justify-between">\n          <div class="flex items-center gap-3">\n            <button onclick="toggleMobileSidebar()" class="md:hidden p-2 rounded-lg hover:bg-surface-100"><i data-lucide="menu" style="width:20px;height:20px;" class="text-ink-300">\n            <h1 id="gestorPageTitle" class="text-lg font-700 text-ink-600">Dashboard\n          \n          <div class="flex items-center gap-2">\n            <span class="text-sm text-ink-200" id="currentDate">\n          \n        \n        <div class="flex-1 overflow-auto p-6" id="gestorContent">\n      \n    \n    \n    <div id="sidebarOverlay" class="fixed inset-0 bg-black/30 z-40 hidden md:hidden" onclick="toggleMobileSidebar()">\n  \n\n  \n  <div id="operadorPanel" class="h-full hidden">\n    <div class="flex flex-col h-full">\n      <header class="bg-white border-b border-surface-200 px-4 py-3 flex items-center justify-between">\n        <div class="flex items-center gap-3">\n          <div class="w-9 h-9 bg-brand-500 rounded-xl flex items-center justify-center">\n            <i data-lucide="wrench" class="text-white" style="width:18px;height:18px;">\n          \n          \n            <h2 id="opTitle" class="font-700 text-ink-600 text-sm">Auto Wrap Pro\n            <p class="text-xs text-ink-100">Painel do Operador\n          \n        \n        <button onclick="logout()" class="p-2 rounded-lg hover:bg-surface-100"><i data-lucide="log-out" style="width:18px;height:18px;" class="text-ink-200">\n      \n      <div class="flex-1 overflow-auto" id="operadorContent">\n      <nav class="bg-white border-t border-surface-200 flex justify-around py-2 px-2">\n        <button onclick="setOpTab('available')" class="op-tab-btn flex flex-col items-center gap-1 py-2 px-3 rounded-xl" data-tab="available">\n          <i data-lucide="clipboard-list" style="width:20px;height:20px;">\n          <span class="text-xs font-500">Disponíveis\n        \n        <button onclick="setOpTab('mine')" class="op-tab-btn flex flex-col items-center gap-1 py-2 px-3 rounded-xl" data-tab="mine">\n          <i data-lucide="user-check" style="width:20px;height:20px;">\n          <span class="text-xs font-500">Minhas OS\n        \n        <button onclick="setOpTab('checklist')" class="op-tab-btn flex flex-col items-center gap-1 py-2 px-3 rounded-xl" data-tab="checklist">\n          <i data-lucide="check-square" style="width:20px;height:20px;">\n          <span class="text-xs font-500">Checklist\n        \n      \n    \n  \n\n  \n  <div id="tvPanel" class="h-full hidden" style="background: linear-gradient(180deg, #0f172a 0%, #1e293b 100%);">\n    <div class="flex flex-col h-full p-6">\n      <header class="flex items-center justify-between mb-6">\n        <div class="flex items-center gap-4">\n          <div class="w-12 h-12 bg-brand-500 rounded-2xl flex items-center justify-center shadow-lg shadow-brand-500/30">\n            <i data-lucide="car" class="text-white" style="width:24px;height:24px;">\n          \n          \n            <h1 id="tvTitle" class="text-2xl font-800 text-white">Auto Wrap Pro\n            <p class="text-sm text-slate-400">Painel de Acompanhamento em Tempo Real\n          \n        \n        <div class="flex items-center gap-4">\n          <div class="text-right">\n            <p id="tvTime" class="text-3xl font-700 text-white">\n            <p id="tvDate" class="text-sm text-slate-400">\n          \n          <button onclick="logout()" class="p-2 rounded-lg hover:bg-white/10 transition"><i data-lucide="x" style="width:20px;height:20px;" class="text-slate-400">\n        \n      \n      <div class="grid grid-cols-4 gap-4 mb-6" id="tvStats">\n      <div class="flex-1 overflow-hidden rounded-2xl bg-white/5 backdrop-blur border border-white/10">\n        <div class="p-4 border-b border-white/10 flex items-center justify-between">\n          <h3 class="text-white font-600 flex items-center gap-2"><i data-lucide="activity" style="width:18px;height:18px;" class="text-brand-400"> Ordens em Execução\n          <span class="tv-pulse flex items-center gap-2 text-brand-400 text-sm font-500"><span class="w-2 h-2 bg-brand-400 rounded-full"> AO VIVO\n        \n        <div class="overflow-auto" style="height:calc(100% - 56px);" id="tvTable">\n      \n    \n  \n\n  \n  <div id="modalContainer" class="fixed inset-0 z-50 hidden">\n\n  \n  <div id="confirmContainer" class="fixed inset-0 z-50 hidden">\n\n\n\n\n// ===== STATE =====\nlet allData = [];\nlet currentRole = null;\nlet gestorPage = 'dashboard';\nlet opTab = 'available';\nlet currentOperator = 'Operador 1';\nlet recordCount = 0;\n\nconst defaultConfig = {\n  app_title: 'Auto Wrap Pro',\n  company_name: '',\n  welcome_text: 'Bem-vindo ao sistema de gestão',\n  primary_color: '#22c55e',\n  surface_color: '#ffffff',\n  text_color: '#1e293b',\n  action_color: '#22c55e',\n  secondary_action_color: '#e9ecf0',\n  font_family: 'Plus Jakarta Sans',\n  font_size: 14\n};\n\nconst GESTOR_MENU = [\n  { id:'dashboard', icon:'layout-dashboard', label:'Dashboard' },\n  { id:'clients', icon:'users', label:'Clientes' },\n  { id:'services', icon:'wrench', label:'Serviços' },\n  { id:'materials', icon:'package', label:'Materiais' },\n  { id:'collaborators', icon:'hard-hat', label:'Colaboradores' },\n  { id:'quotes', icon:'file-text', label:'Orçamentos' },\n  { id:'orders', icon:'clipboard-list', label:'Ordens de Serviço' },\n  { id:'schedule', icon:'calendar', label:'Agenda' },\n];\n\nconst SERVICE_CATEGORIES = ['PPF','Insufilm','Envelopamento','Adesivagem'];\nconst ORDER_STATUSES = ['Orçamento','Aprovado','Em Execução','Concluído','Cancelado'];\nconst CHECKLIST_ITEMS = ['Verificar pintura','Lavar veículo','Preparar superfície','Aplicar material','Revisar acabamento','Limpeza final','Fotos antes/depois','Entrega ao cliente'];\n\n// ===== HELPERS =====\nfunction getByType(type) { return allData.filter(d => d.type === type); }\nfunction toast(msg, type='success') {\n  const c = document.getElementById('toastContainer');\n  const colors = { success:'bg-brand-500', error:'bg-red-500', info:'bg-blue-500' };\n  const el = document.createElement('div');\n  el.className = ${colors[type]||colors.info} text-white px-4 py-3 rounded-xl shadow-lg text-sm font-500 toast-anim flex items-center gap-2;\n  el.innerHTML = <i data-lucide=\"${type==='success'?'check-circle':type==='error'?'alert-circle':'info'}\" style=\"width:16px;height:16px;\"></i> ${msg};\n  c.appendChild(el);\n  lucide.createIcons();\n  setTimeout(() => { el.style.opacity='0'; el.style.transition='opacity 0.3s'; setTimeout(() => el.remove(), 300); }, 3000);\n}\n\nfunction showConfirm(msg, onConfirm) {\n  const c = document.getElementById('confirmContainer');\n  c.classList.remove('hidden');\n  c.innerHTML = <div class=\"modal-overlay absolute inset-0 flex items-center justify-center p-4\">\n    <div class=\"bg-white rounded-2xl p-6 max-w-sm w-full shadow-2xl fade-in text-center\">\n      <div class=\"w-12 h-12 bg-red-100 rounded-full flex items-center justify-center mx-auto mb-4\">\n        <i data-lucide=\"alert-triangle\" style=\"width:24px;height:24px;\" class=\"text-red-500\"></i>\n      </div>\n      <p class=\"text-ink-500 font-600 mb-2\">${msg}</p>\n      <p class=\"text-ink-200 text-sm mb-6\">Esta ação não pode ser desfeita.</p>\n      <div class=\"flex gap-3\">\n        <button onclick=\"closeConfirm()\" class=\"flex-1 btn-secondary rounded-xl py-2.5 font-500 text-sm\">Cancelar</button>\n        <button id=\"confirmBtn\" class=\"flex-1 bg-red-500 text-white rounded-xl py-2.5 font-500 text-sm hover:bg-red-600 transition\">Confirmar</button>\n      </div>\n    </div>\n  </div>;\n  document.getElementById('confirmBtn').onclick = () => { closeConfirm(); onConfirm(); };\n  lucide.createIcons();\n}\nfunction closeConfirm() { document.getElementById('confirmContainer').classList.add('hidden'); document.getElementById('confirmContainer').innerHTML = ''; }\n\nfunction formatDate(iso) { if(!iso) return '-'; try { const d=new Date(iso); return d.toLocaleDateString('pt-BR'); } catch(e) { return '-'; } }\nfunction formatCurrency(v) { const n = parseFloat(v); if(isNaN(n)) return 'R$ 0,00'; return 'R$ ' + n.toFixed(2).replace('.',',').replace(/\B(?=(\d{3})+(?!\d))/g,'.'); }\nfunction uid() { return Date.now().toString(36) + Math.random().toString(36).substr(2,5); }\n\n// ===== MODAL =====\nfunction showModal(title, content, width='max-w-lg') {\n  const m = document.getElementById('modalContainer');\n  m.classList.remove('hidden');\n  m.innerHTML = <div class=\"modal-overlay absolute inset-0 flex items-start justify-center p-4 overflow-auto\" onclick=\"if(event.target===this)closeModal()\">\n    <div class=\"bg-white rounded-2xl shadow-2xl w-full ${width} my-8 fade-in\">\n      <div class=\"flex items-center justify-between p-5 border-b border-surface-200\">\n        <h3 class=\"font-700 text-ink-600\">${title}</h3>\n        <button onclick=\"closeModal()\" class=\"p-1.5 rounded-lg hover:bg-surface-100 transition\"><i data-lucide=\"x\" style=\"width:18px;height:18px;\" class=\"text-ink-200\"></i></button>\n      </div>\n      <div class=\"p-5\">${content}</div>\n    </div>\n  </div>;\n  lucide.createIcons();\n}\nfunction closeModal() { document.getElementById('modalContainer').classList.add('hidden'); document.getElementById('modalContainer').innerHTML = ''; }\n\n// ===== NAV =====\nfunction enterAs(role) {\n  currentRole = role;\n  document.getElementById('loginScreen').classList.add('hidden');\n  document.getElementById('gestorPanel').classList.add('hidden');\n  document.getElementById('operadorPanel').classList.add('hidden');\n  document.getElementById('tvPanel').classList.add('hidden');\n  if(role==='gestor') { document.getElementById('gestorPanel').classList.remove('hidden'); renderGestorNav(); setGestorPage('dashboard'); }\n  else if(role==='operador') { document.getElementById('operadorPanel').classList.remove('hidden'); setOpTab('available'); }\n  else if(role==='tv') { document.getElementById('tvPanel').classList.remove('hidden'); renderTV(); startTVClock(); }\n}\nfunction logout() {\n  currentRole = null;\n  document.getElementById('loginScreen').classList.remove('hidden');\n  document.getElementById('gestorPanel').classList.add('hidden');\n  document.getElementById('operadorPanel').classList.add('hidden');\n  document.getElementById('tvPanel').classList.add('hidden');\n}\nfunction toggleMobileSidebar() {\n  document.getElementById('gestorSidebar').classList.toggle('open');\n  document.getElementById('sidebarOverlay').classList.toggle('hidden');\n}\n\n// ===== GESTOR NAV =====\nfunction renderGestorNav() {\n  const nav = document.getElementById('gestorNav');\n  nav.innerHTML = GESTOR_MENU.map(m => <div class=\"sidebar-item ${gestorPage===m.id?'active':''}\" onclick=\"setGestorPage('${m.id}')\">\n    <i data-lucide=\"${m.icon}\" style=\"width:18px;height:18px;\"></i> ${m.label}\n  </div>).join('');\n  lucide.createIcons();\n}\nfunction setGestorPage(page) {\n  gestorPage = page;\n  document.getElementById('gestorPageTitle').textContent = GESTOR_MENU.find(m=>m.id===page)?.label || 'Dashboard';\n  renderGestorNav();\n  renderGestorContent();\n  // close mobile sidebar\n  document.getElementById('gestorSidebar').classList.remove('open');\n  document.getElementById('sidebarOverlay').classList.add('hidden');\n}\n\nfunction renderGestorContent() {\n  const c = document.getElementById('gestorContent');\n  switch(gestorPage) {\n    case 'dashboard': c.innerHTML = renderDashboard(); break;\n    case 'clients': c.innerHTML = renderClients(); break;\n    case 'services': c.innerHTML = renderServices(); break;\n    case 'materials': c.innerHTML = renderMaterials(); break;\n    case 'collaborators': c.innerHTML = renderCollaborators(); break;\n    case 'quotes': c.innerHTML = renderQuotes(); break;\n    case 'orders': c.innerHTML = renderOrders(); break;\n    case 'schedule': c.innerHTML = renderSchedule(); break;\n    default: c.innerHTML = renderDashboard();\n  }\n  lucide.createIcons();\n}\n\n// ===== DASHBOARD =====\nfunction renderDashboard() {\n  const clients = getByType('client');\n  const orders = getByType('order');\n  const quotes = orders.filter(o => o.order_is_quote === 'true');\n  const activeOrders = orders.filter(o => o.order_is_quote !== 'true' && o.order_status !== 'Cancelado');\n  const inProgress = activeOrders.filter(o => o.order_status === 'Em Execução');\n  const completed = activeOrders.filter(o => o.order_status === 'Concluído');\n  const revenue = completed.reduce((s,o) => s + (parseFloat(o.order_total)||0), 0);\n\n  const stats = [\n    { icon:'users', label:'Clientes', value:clients.length, color:'blue' },\n    { icon:'file-text', label:'Orçamentos', value:quotes.length, color:'yellow' },\n    { icon:'clipboard-list', label:'OS Ativas', value:inProgress.length, color:'green' },\n    { icon:'trending-up', label:'Faturamento', value:formatCurrency(revenue), color:'purple' },\n  ];\n  const colorMap = { blue:'bg-blue-50 text-blue-600', yellow:'bg-amber-50 text-amber-600', green:'bg-brand-50 text-brand-600', purple:'bg-purple-50 text-purple-600' };\n\n  return <div class=\"fade-in space-y-6\">\n    <div class=\"grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4\">\n      ${stats.map(s => <div class="bg-white rounded-2xl p-5 border border-surface-200 card-hover">\n        <div class="flex items-center justify-between mb-3">\n          <div class="w-10 h-10 ${colorMap[s.color]} rounded-xl flex items-center justify-center"><i data-lucide="${s.icon}" style="width:20px;height:20px;">\n        \n        <p class="text-2xl font-800 text-ink-600">${s.value}\n        <p class="text-sm text-ink-200">${s.label}\n      ).join('')}\n    </div>\n    <div class=\"grid grid-cols-1 lg:grid-cols-2 gap-4\">\n      <div class=\"bg-white rounded-2xl p-5 border border-surface-200\">\n        <h3 class=\"font-700 text-ink-500 mb-4 flex items-center gap-2\"><i data-lucide=\"clock\" style=\"width:18px;height:18px;\" class=\"text-brand-500\"></i> Ordens Recentes</h3>\n        ${activeOrders.length === 0 ? '<p class=\"text-ink-100 text-sm\">Nenhuma ordem ainda.</p>' :\n        activeOrders.slice(-5).reverse().map(o => <div class="flex items-center justify-between py-3 border-b border-surface-100 last:border-0">\n          <p class="font-500 text-ink-500 text-sm">${o.order_client_name||'-'}<p class="text-xs text-ink-100">${o.order_vehicle||'-'} • ${o.order_plate||'-'}\n          <span class="badge ${statusBadge(o.order_status)}">${o.order_status||'-'}\n        ).join('')}\n      </div>\n      <div class=\"bg-white rounded-2xl p-5 border border-surface-200\">\n        <h3 class=\"font-700 text-ink-500 mb-4 flex items-center gap-2\"><i data-lucide=\"pie-chart\" style=\"width:18px;height:18px;\" class=\"text-brand-500\"></i> Por Categoria</h3>\n        ${SERVICE_CATEGORIES.map(cat => {\n          const count = activeOrders.filter(o => (o.order_services||'').includes(cat)).length;\n          const pct = activeOrders.length ? Math.round(count/activeOrders.length*100) : 0;\n          return <div class="mb-3"><div class="flex justify-between text-sm mb-1"><span class="text-ink-300 font-500">${cat}<span class="text-ink-200">${count}\n          <div class="h-2 bg-surface-200 rounded-full overflow-hidden"><div class="h-full bg-brand-400 rounded-full transition-all" style="width:${pct}%">;\n        }).join('')}\n      </div>\n    </div>\n  </div>;\n}\nfunction statusBadge(s) {\n  const m = { 'Orçamento':'badge-gray','Aprovado':'badge-blue','Em Execução':'badge-yellow','Concluído':'badge-green','Cancelado':'badge-red' };\n  return m[s] || 'badge-gray';\n}\n\n// ===== CLIENTS =====\nfunction renderClients() {\n  const clients = getByType('client');\n  return <div class=\"fade-in\">\n    <div class=\"flex items-center justify-between mb-4\">\n      <p class=\"text-sm text-ink-200\">${clients.length} clientes cadastrados</p>\n      <button onclick=\"openClientForm()\" class=\"btn-primary rounded-xl px-4 py-2.5 text-sm font-600 flex items-center gap-2\"><i data-lucide=\"plus\" style=\"width:16px;height:16px;\"></i> Novo Cliente</button>\n    </div>\n    ${clients.length === 0 ? emptyState('users','Nenhum cliente cadastrado','Adicione seu primeiro cliente') :\n    <div class="bg-white rounded-2xl border border-surface-200 overflow-hidden"><div class="overflow-x-auto"><table class="w-full text-sm">\n      <tr class="bg-surface-50 text-ink-200 text-left"><th class="py-3 px-4 font-600">Nome<th class="py-3 px-4 font-600 hidden sm:table-cell">Telefone<th class="py-3 px-4 font-600 hidden md:table-cell">Veículo<th class="py-3 px-4 font-600 hidden md:table-cell">Placa<th class="py-3 px-4 font-600">Ações\n      ${clients.map(cl => <tr class=\"table-row border-t border-surface-100\">\n        <td class=\"py-3 px-4\"><p class=\"font-500 text-ink-500\">${cl.name||'-'}</p><p class=\"text-xs text-ink-100 sm:hidden\">${cl.phone||''}</p></td>\n        <td class=\"py-3 px-4 hidden sm:table-cell text-ink-300\">${cl.phone||'-'}</td>\n        <td class=\"py-3 px-4 hidden md:table-cell text-ink-300\">${cl.vehicle||'-'}</td>\n        <td class=\"py-3 px-4 hidden md:table-cell\"><span class=\"bg-surface-100 px-2 py-1 rounded-lg text-xs font-600 text-ink-400\">${cl.plate||'-'}</span></td>\n        <td class=\"py-3 px-4\"><div class=\"flex gap-1\">\n          <button onclick=\"openClientForm('${cl.__backendId}')\" class=\"p-1.5 rounded-lg hover:bg-surface-100\"><i data-lucide=\"edit-2\" style=\"width:15px;height:15px;\" class=\"text-ink-200\"></i></button>\n          <button onclick=\"deleteRecord('${cl.__backendId}','${(cl.name||'').replace(/'/g,\"\\\\'\")}',' cliente')\" class=\"p-1.5 rounded-lg hover:bg-red-50\"><i data-lucide=\"trash-2\" style=\"width:15px;height:15px;\" class=\"text-red-400\"></i></button>\n          <button onclick=\"viewClientHistory('${cl.__backendId}')\" class=\"p-1.5 rounded-lg hover:bg-blue-50\" title=\"Histórico\"><i data-lucide=\"history\" style=\"width:15px;height:15px;\" class=\"text-blue-400\"></i></button>\n        </div></td>\n      </tr>).join('')}}\n  </div>;\n}\n\nfunction openClientForm(editId) {\n  const cl = editId ? allData.find(d => d.__backendId === editId) : null;\n  showModal(cl ? 'Editar Cliente' : 'Novo Cliente', <form id=\"clientForm\" class=\"space-y-3\" onsubmit=\"event.preventDefault(); saveClient('${editId||''}')\">\n    <div class=\"grid grid-cols-1 sm:grid-cols-2 gap-3\">\n      <div><label class=\"text-xs font-600 text-ink-300 mb-1 block\">Nome *</label><input class=\"input-field\" id=\"cf_name\" required value=\"${cl?.name||''}\"></div>\n      <div><label class=\"text-xs font-600 text-ink-300 mb-1 block\">CPF/CNPJ</label><input class=\"input-field\" id=\"cf_cpf\" value=\"${cl?.cpf||''}\"></div>\n      <div><label class=\"text-xs font-600 text-ink-300 mb-1 block\">Telefone</label><input class=\"input-field\" id=\"cf_phone\" value=\"${cl?.phone||''}\"></div>\n      <div><label class=\"text-xs font-600 text-ink-300 mb-1 block\">Email</label><input class=\"input-field\" id=\"cf_email\" type=\"email\" value=\"${cl?.email||''}\"></div>\n      <div><label class=\"text-xs font-600 text-ink-300 mb-1 block\">Veículo</label><input class=\"input-field\" id=\"cf_vehicle\" value=\"${cl?.vehicle||''}\"></div>\n      <div><label class=\"text-xs font-600 text-ink-300 mb-1 block\">Placa</label><input class=\"input-field\" id=\"cf_plate\" value=\"${cl?.plate||''}\"></div>\n      <div><label class=\"text-xs font-600 text-ink-300 mb-1 block\">Cor</label><input class=\"input-field\" id=\"cf_color\" value=\"${cl?.vehicle_color||''}\"></div>\n      <div><label class=\"text-xs font-600 text-ink-300 mb-1 block\">Ano</label><input class=\"input-field\" id=\"cf_year\" value=\"${cl?.vehicle_year||''}\"></div>\n    </div>\n    <div class=\"flex justify-end gap-2 pt-2\">\n      <button type=\"button\" onclick=\"closeModal()\" class=\"btn-secondary rounded-xl px-4 py-2.5 text-sm font-500\">Cancelar</button>\n      <button type=\"submit\" class=\"btn-primary rounded-xl px-5 py-2.5 text-sm font-600\" id=\"clientSaveBtn\">Salvar</button>\n    </div>\n  </form>);\n}\n\nasync function saveClient(editId) {\n  const btn = document.getElementById('clientSaveBtn');\n  btn.disabled = true; btn.textContent = 'Salvando...';\n  const data = { type:'client', name:document.getElementById('cf_name').value, cpf:document.getElementById('cf_cpf').value, phone:document.getElementById('cf_phone').value, email:document.getElementById('cf_email').value, vehicle:document.getElementById('cf_vehicle').value, plate:document.getElementById('cf_plate').value, vehicle_color:document.getElementById('cf_color').value, vehicle_year:document.getElementById('cf_year').value, created_at: new Date().toISOString() };\n  let result;\n  if(editId) {\n    const existing = allData.find(d => d.__backendId === editId);\n    if(existing) result = await window.dataSdk.update({...existing, ...data});\n  } else {\n    if(recordCount >= 999) { toast('Limite de registros atingido','error'); btn.disabled=false; btn.textContent='Salvar'; return; }\n    result = await window.dataSdk.create(data);\n  }\n  if(result?.isOk) { toast(editId?'Cliente atualizado!':'Cliente cadastrado!'); closeModal(); }\n  else { toast('Erro ao salvar','error'); btn.disabled=false; btn.textContent='Salvar'; }\n}\n\nfunction viewClientHistory(clientId) {\n  const cl = allData.find(d => d.__backendId === clientId);\n  if(!cl) return;\n  const orders = getByType('order').filter(o => o.order_client_name === cl.name);\n  showModal(Histórico - ${cl.name}, \n    <div class=\"mb-4 p-4 bg-surface-50 rounded-xl\">\n      <p class=\"text-sm text-ink-300\"><strong>Veículo:</strong> ${cl.vehicle||'-'} • <strong>Placa:</strong> ${cl.plate||'-'}</p>\n    </div>\n    ${orders.length === 0 ? '<p class=\"text-ink-100 text-sm\">Nenhum serviço realizado.</p>' :\n    orders.map(o => <div class="p-3 border border-surface-200 rounded-xl mb-2"><div class="flex justify-between items-start">\n      <p class="font-500 text-ink-500 text-sm">${o.order_services||'-'}<p class="text-xs text-ink-100">${formatDate(o.order_date)}\n      <span class="badge ${statusBadge(o.order_status)}">${o.order_status||'-'}\n    <p class="text-sm text-ink-300 mt-1">${formatCurrency(o.order_total)}).join('')}\n  , 'max-w-md');\n}\n\n// ===== SERVICES =====\nfunction renderServices() {\n  const svcs = getByType('service');\n  return <div class=\"fade-in\">\n    <div class=\"flex items-center justify-between mb-4\">\n      <p class=\"text-sm text-ink-200\">${svcs.length} serviços cadastrados</p>\n      <button onclick=\"openServiceForm()\" class=\"btn-primary rounded-xl px-4 py-2.5 text-sm font-600 flex items-center gap-2\"><i data-lucide=\"plus\" style=\"width:16px;height:16px;\"></i> Novo Serviço</button>\n    </div>\n    ${svcs.length === 0 ? emptyState('wrench','Nenhum serviço cadastrado','Adicione os tipos de serviço') :\n    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-3">${svcs.map(s => <div class=\"bg-white rounded-2xl p-4 border border-surface-200 card-hover\">\n      <div class=\"flex items-start justify-between\">\n        <div><span class=\"badge ${catBadge(s.service_category)}\">${s.service_category||'-'}</span><h4 class=\"font-600 text-ink-500 mt-2\">${s.service_name||'-'}</h4></div>\n        <div class=\"flex gap-1\">\n          <button onclick=\"openServiceForm('${s.__backendId}')\" class=\"p-1.5 rounded-lg hover:bg-surface-100\"><i data-lucide=\"edit-2\" style=\"width:14px;height:14px;\" class=\"text-ink-200\"></i></button>\n          <button onclick=\"deleteRecord('${s.__backendId}','${(s.service_name||'').replace(/'/g,\"\\\\'\")}',' serviço')\" class=\"p-1.5 rounded-lg hover:bg-red-50\"><i data-lucide=\"trash-2\" style=\"width:14px;height:14px;\" class=\"text-red-400\"></i></button>\n        </div>\n      </div>\n      <p class=\"text-lg font-700 text-brand-600 mt-2\">${formatCurrency(s.service_price)}</p>\n    </div>).join('')}}\n  </div>;\n}\nfunction catBadge(c) { const m={'PPF':'badge-blue','Insufilm':'badge-purple','Envelopamento':'badge-green','Adesivagem':'badge-yellow'}; return m[c]||'badge-gray'; }\n\nfunction openServiceForm(editId) {\n  const s = editId ? allData.find(d => d.__backendId === editId) : null;\n  showModal(s?'Editar Serviço':'Novo Serviço', <form id=\"svcForm\" class=\"space-y-3\" onsubmit=\"event.preventDefault();saveService('${editId||''}')\">\n    <div><label class=\"text-xs font-600 text-ink-300 mb-1 block\">Nome do Serviço *</label><input class=\"input-field\" id=\"sf_name\" required value=\"${s?.service_name||''}\"></div>\n    <div><label class=\"text-xs font-600 text-ink-300 mb-1 block\">Categoria *</label><select class=\"input-field\" id=\"sf_cat\" required>\n      <option value=\"\">Selecione...</option>${SERVICE_CATEGORIES.map(c => <option ${s?.service_category===c?'selected':''}>${c}).join('')}\n    </select></div>\n    <div><label class=\"text-xs font-600 text-ink-300 mb-1 block\">Preço (R$)</label><input class=\"input-field\" id=\"sf_price\" type=\"number\" step=\"0.01\" value=\"${s?.service_price||''}\"></div>\n    <div class=\"flex justify-end gap-2 pt-2\">\n      <button type=\"button\" onclick=\"closeModal()\" class=\"btn-secondary rounded-xl px-4 py-2.5 text-sm font-500\">Cancelar</button>\n      <button type=\"submit\" class=\"btn-primary rounded-xl px-5 py-2.5 text-sm font-600\" id=\"svcSaveBtn\">Salvar</button>\n    </div>\n  </form>, 'max-w-sm');\n}\nasync function saveService(editId) {\n  const btn = document.getElementById('svcSaveBtn'); btn.disabled=true; btn.textContent='Salvando...';\n  const data = { type:'service', service_name:document.getElementById('sf_name').value, service_category:document.getElementById('sf_cat').value, service_price:document.getElementById('sf_price').value, created_at:new Date().toISOString() };\n  let result;\n  if(editId) { const ex = allData.find(d=>d.__backendId===editId); if(ex) result = await window.dataSdk.update({...ex,...data}); }\n  else { if(recordCount>=999){toast('Limite atingido','error');btn.disabled=false;btn.textContent='Salvar';return;} result = await window.dataSdk.create(data); }\n  if(result?.isOk){toast(editId?'Serviço atualizado!':'Serviço cadastrado!');closeModal();}else{toast('Erro ao salvar','error');btn.disabled=false;btn.textContent='Salvar';}\n}\n\n// ===== MATERIALS =====\nfunction renderMaterials() {\n  const mats = getByType('material');\n  return <div class=\"fade-in\">\n    <div class=\"flex items-center justify-between mb-4\">\n      <p class=\"text-sm text-ink-200\">${mats.length} materiais</p>\n      <button onclick=\"openMaterialForm()\" class=\"btn-primary rounded-xl px-4 py-2.5 text-sm font-600 flex items-center gap-2\"><i data-lucide=\"plus\" style=\"width:16px;height:16px;\"></i> Novo Material</button>\n    </div>\n    ${mats.length === 0 ? emptyState('package','Nenhum material cadastrado','Registre seus materiais') :\n    <div class="bg-white rounded-2xl border border-surface-200 overflow-hidden"><div class="overflow-x-auto"><table class="w-full text-sm">\n      <tr class="bg-surface-50 text-ink-200 text-left"><th class="py-3 px-4 font-600">Material<th class="py-3 px-4 font-600">Marca<th class="py-3 px-4 font-600">Qtd<th class="py-3 px-4 font-600">Ações\n      ${mats.map(m => <tr class=\"table-row border-t border-surface-100\">\n        <td class=\"py-3 px-4 font-500 text-ink-500\">${m.material_name||'-'}</td>\n        <td class=\"py-3 px-4 text-ink-300\">${m.material_brand||'-'}</td>\n        <td class=\"py-3 px-4 text-ink-300\">${m.material_qty||'0'} ${m.material_unit||''}</td>\n        <td class=\"py-3 px-4\"><div class=\"flex gap-1\">\n          <button onclick=\"openMaterialForm('${m.__backendId}')\" class=\"p-1.5 rounded-lg hover:bg-surface-100\"><i data-lucide=\"edit-2\" style=\"width:15px;height:15px;\" class=\"text-ink-200\"></i></button>\n          <button onclick=\"deleteRecord('${m.__backendId}','${(m.material_name||'').replace(/'/g,\"\\\\'\")}',' material')\" class=\"p-1.5 rounded-lg hover:bg-red-50\"><i data-lucide=\"trash-2\" style=\"width:15px;height:15px;\" class=\"text-red-400\"></i></button>\n        </div></td>\n      </tr>).join('')}}\n  </div>;\n}\nfunction openMaterialForm(editId) {\n  const m = editId ? allData.find(d=>d.__backendId===editId) : null;\n  showModal(m?'Editar Material':'Novo Material', <form class=\"space-y-3\" onsubmit=\"event.preventDefault();saveMaterial('${editId||''}')\">\n    <div><label class=\"text-xs font-600 text-ink-300 mb-1 block\">Nome *</label><input class=\"input-field\" id=\"mf_name\" required value=\"${m?.material_name||''}\"></div>\n    <div class=\"grid grid-cols-2 gap-3\">\n      <div><label class=\"text-xs font-600 text-ink-300 mb-1 block\">Marca</label><input class=\"input-field\" id=\"mf_brand\" value=\"${m?.material_brand||''}\"></div>\n      <div><label class=\"text-xs font-600 text-ink-300 mb-1 block\">Qtd</label><input class=\"input-field\" id=\"mf_qty\" type=\"number\" value=\"${m?.material_qty||''}\"></div>\n    </div>\n    <div><label class=\"text-xs font-600 text-ink-300 mb-1 block\">Unidade</label><input class=\"input-field\" id=\"mf_unit\" placeholder=\"m², un, rolo...\" value=\"${m?.material_unit||''}\"></div>\n    <div class=\"flex justify-end gap-2 pt-2\">\n      <button type=\"button\" onclick=\"closeModal()\" class=\"btn-secondary rounded-xl px-4 py-2.5 text-sm font-500\">Cancelar</button>\n      <button type=\"submit\" class=\"btn-primary rounded-xl px-5 py-2.5 text-sm font-600\" id=\"matSaveBtn\">Salvar</button>\n    </div>\n  </form>, 'max-w-sm');\n}\nasync function saveMaterial(editId) {\n  const btn=document.getElementById('matSaveBtn');btn.disabled=true;btn.textContent='Salvando...';\n  const data={type:'material',material_name:document.getElementById('mf_name').value,material_brand:document.getElementById('mf_brand').value,material_qty:document.getElementById('mf_qty').value,material_unit:document.getElementById('mf_unit').value,created_at:new Date().toISOString()};\n  let result;\n  if(editId){const ex=allData.find(d=>d.__backendId===editId);if(ex)result=await window.dataSdk.update({...ex,...data});}\n  else{if(recordCount>=999){toast('Limite atingido','error');btn.disabled=false;btn.textContent='Salvar';return;}result=await window.dataSdk.create(data);}\n  if(result?.isOk){toast(editId?'Material atualizado!':'Material cadastrado!');closeModal();}else{toast('Erro','error');btn.disabled=false;btn.textContent='Salvar';}\n}\n\n// ===== COLLABORATORS =====\nfunction renderCollaborators() {\n  const cols = getByType('collaborator');\n  return <div class=\"fade-in\">\n    <div class=\"flex items-center justify-between mb-4\">\n      <p class=\"text-sm text-ink-200\">${cols.length} colaboradores</p>\n      <button onclick=\"openCollabForm()\" class=\"btn-primary rounded-xl px-4 py-2.5 text-sm font-600 flex items-center gap-2\"><i data-lucide=\"plus\" style=\"width:16px;height:16px;\"></i> Novo Colaborador</button>\n    </div>\n    ${cols.length===0 ? emptyState('hard-hat','Nenhum colaborador','Cadastre sua equipe') :\n    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-3">${cols.map(c => <div class=\"bg-white rounded-2xl p-4 border border-surface-200 card-hover\">\n      <div class=\"flex items-center gap-3\">\n        <div class=\"w-11 h-11 bg-brand-100 rounded-xl flex items-center justify-center text-brand-600 font-700\">${(c.collab_name||'?')[0].toUpperCase()}</div>\n        <div class=\"flex-1\">\n          <h4 class=\"font-600 text-ink-500\">${c.collab_name||'-'}</h4>\n          <p class=\"text-xs text-ink-100\">${c.collab_role||'Operador'}</p>\n        </div>\n        <div class=\"flex gap-1\">\n          <button onclick=\"openCollabForm('${c.__backendId}')\" class=\"p-1.5 rounded-lg hover:bg-surface-100\"><i data-lucide=\"edit-2\" style=\"width:14px;height:14px;\" class=\"text-ink-200\"></i></button>\n          <button onclick=\"deleteRecord('${c.__backendId}','${(c.collab_name||'').replace(/'/g,\"\\\\'\")}',' colaborador')\" class=\"p-1.5 rounded-lg hover:bg-red-50\"><i data-lucide=\"trash-2\" style=\"width:14px;height:14px;\" class=\"text-red-400\"></i></button>\n        </div>\n      </div>\n    </div>).join('')}}\n  </div>;\n}\nfunction openCollabForm(editId) {\n  const c = editId ? allData.find(d=>d.__backendId===editId) : null;\n  showModal(c?'Editar Colaborador':'Novo Colaborador', <form class=\"space-y-3\" onsubmit=\"event.preventDefault();saveCollab('${editId||''}')\">\n    <div><label class=\"text-xs font-600 text-ink-300 mb-1 block\">Nome *</label><input class=\"input-field\" id=\"clf_name\" required value=\"${c?.collab_name||''}\"></div>\n    <div><label class=\"text-xs font-600 text-ink-300 mb-1 block\">Função</label><input class=\"input-field\" id=\"clf_role\" placeholder=\"Aplicador, Instalador...\" value=\"${c?.collab_role||''}\"></div>\n    <div><label class=\"text-xs font-600 text-ink-300 mb-1 block\">PIN de Acesso</label><input class=\"input-field\" id=\"clf_pin\" type=\"text\" maxlength=\"4\" placeholder=\"4 dígitos\" value=\"${c?.collab_pin||''}\"></div>\n    <div class=\"flex justify-end gap-2 pt-2\">\n      <button type=\"button\" onclick=\"closeModal()\" class=\"btn-secondary rounded-xl px-4 py-2.5 text-sm font-500\">Cancelar</button>\n      <button type=\"submit\" class=\"btn-primary rounded-xl px-5 py-2.5 text-sm font-600\" id=\"collSaveBtn\">Salvar</button>\n    </div>\n  </form>, 'max-w-sm');\n}\nasync function saveCollab(editId) {\n  const btn=document.getElementById('collSaveBtn');btn.disabled=true;btn.textContent='Salvando...';\n  const data={type:'collaborator',collab_name:document.getElementById('clf_name').value,collab_role:document.getElementById('clf_role').value,collab_pin:document.getElementById('clf_pin').value,created_at:new Date().toISOString()};\n  let result;\n  if(editId){const ex=allData.find(d=>d.__backendId===editId);if(ex)result=await window.dataSdk.update({...ex,...data});}\n  else{if(recordCount>=999){toast('Limite atingido','error');btn.disabled=false;btn.textContent='Salvar';return;}result=await window.dataSdk.create(data);}\n  if(result?.isOk){toast(editId?'Colaborador atualizado!':'Colaborador cadastrado!');closeModal();}else{toast('Erro','error');btn.disabled=false;btn.textContent='Salvar';}\n}\n\n// ===== QUOTES & ORDERS =====\nfunction renderQuotes() { return renderOrderList(true); }\nfunction renderOrders() { return renderOrderList(false); }\n\nfunction renderOrderList(isQuote) {\n  const orders = getByType('order').filter(o => isQuote ? o.order_is_quote==='true' : o.order_is_quote!=='true');\n  const title = isQuote ? 'orçamentos' : 'ordens de serviço';\n  return <div class=\"fade-in\">\n    <div class=\"flex items-center justify-between mb-4\">\n      <p class=\"text-sm text-ink-200\">${orders.length} ${title}</p>\n      <button onclick=\"openOrderForm(null,${isQuote})\" class=\"btn-primary rounded-xl px-4 py-2.5 text-sm font-600 flex items-center gap-2\"><i data-lucide=\"plus\" style=\"width:16px;height:16px;\"></i> ${isQuote?'Novo Orçamento':'Nova OS'}</button>\n    </div>\n    ${orders.length===0 ? emptyState(isQuote?'file-text':'clipboard-list', Nenhum ${isQuote?'orçamento':'OS'}, Crie o primeiro ${isQuote?'orçamento':'OS'}) :\n    <div class="space-y-3">${orders.map(o => <div class=\"bg-white rounded-2xl p-4 border border-surface-200 card-hover\">\n      <div class=\"flex flex-wrap items-start justify-between gap-2\">\n        <div class=\"flex-1 min-w-0\">\n          <div class=\"flex items-center gap-2 flex-wrap\">\n            <h4 class=\"font-600 text-ink-500\">${o.order_client_name||'-'}</h4>\n            <span class=\"badge ${statusBadge(o.order_status)}\">${o.order_status||'-'}</span>\n          </div>\n          <p class=\"text-sm text-ink-200 mt-1\"><i data-lucide=\"car\" style=\"width:13px;height:13px;display:inline;vertical-align:-2px;\" class=\"text-ink-100\"></i> ${o.order_vehicle||'-'} • ${o.order_plate||'-'}</p>\n          <p class=\"text-sm text-ink-200 mt-1\">${o.order_services||'-'}</p>\n          <div class=\"flex items-center gap-3 mt-2 flex-wrap\">\n            <span class=\"text-xs text-ink-100\"><i data-lucide=\"calendar\" style=\"width:12px;height:12px;display:inline;vertical-align:-2px;\"></i> ${formatDate(o.order_date)}</span>\n            ${o.order_operator?<span class="text-xs text-ink-100"><i data-lucide="user" style="width:12px;height:12px;display:inline;vertical-align:-2px;"> ${o.order_operator}:''}\n          </div>\n        </div>\n        <div class=\"text-right\">\n          <p class=\"text-lg font-700 text-brand-600\">${formatCurrency(o.order_total)}</p>\n          <div class=\"flex gap-1 mt-2 justify-end\">\n            <button onclick=\"openOrderForm('${o.__backendId}',${isQuote})\" class=\"p-1.5 rounded-lg hover:bg-surface-100\"><i data-lucide=\"edit-2\" style=\"width:15px;height:15px;\" class=\"text-ink-200\"></i></button>\n            ${isQuote && o.order_status==='Orçamento'?<button onclick="approveQuote('${o.__backendId}')" class="p-1.5 rounded-lg hover:bg-green-50" title="Aprovar"><i data-lucide="check-circle" style="width:15px;height:15px;" class="text-brand-500">:''}\n            <button onclick=\"openChecklist('${o.__backendId}')\" class=\"p-1.5 rounded-lg hover:bg-blue-50\" title=\"Checklist\"><i data-lucide=\"check-square\" style=\"width:15px;height:15px;\" class=\"text-blue-500\"></i></button>\n            <button onclick=\"deleteRecord('${o.__backendId}','OS',' registro')\" class=\"p-1.5 rounded-lg hover:bg-red-50\"><i data-lucide=\"trash-2\" style=\"width:15px;height:15px;\" class=\"text-red-400\"></i></button>\n          </div>\n        </div>\n      </div>\n    </div>).join('')}}\n  </div>;\n}\n\nfunction openOrderForm(editId, isQuote) {\n  const o = editId ? allData.find(d=>d.__backendId===editId) : null;\n  const clients = getByType('client');\n  const svcs = getByType('service');\n  const collabs = getByType('collaborator');\n  const selectedSvcs = o ? (o.order_services||'').split(', ').filter(Boolean) : [];\n\n  showModal(o ? (isQuote?'Editar Orçamento':'Editar OS') : (isQuote?'Novo Orçamento':'Nova OS'), <form class=\"space-y-3\" onsubmit=\"event.preventDefault();saveOrder('${editId||''}',${isQuote})\">\n    <div class=\"grid grid-cols-1 sm:grid-cols-2 gap-3\">\n      <div class=\"sm:col-span-2\"><label class=\"text-xs font-600 text-ink-300 mb-1 block\">Cliente *</label><select class=\"input-field\" id=\"of_client\" required>\n        <option value=\"\">Selecione...</option>${clients.map(c => <option ${o?.order_client_name===c.name?'selected':''}>${c.name}).join('')}\n      </select></div>\n      <div><label class=\"text-xs font-600 text-ink-300 mb-1 block\">Veículo</label><input class=\"input-field\" id=\"of_vehicle\" value=\"${o?.order_vehicle||''}\"></div>\n      <div><label class=\"text-xs font-600 text-ink-300 mb-1 block\">Placa</label><input class=\"input-field\" id=\"of_plate\" value=\"${o?.order_plate||''}\"></div>\n    </div>\n    <div><label class=\"text-xs font-600 text-ink-300 mb-1 block\">Serviços</label>\n      <div class=\"grid grid-cols-2 gap-2\">${svcs.map(s => <label class="flex items-center gap-2 p-2 border border-surface-200 rounded-lg cursor-pointer hover:bg-surface-50">\n        <input type="checkbox" class="svc-check accent-green-500" value="${s.service_name}" data-price="${s.service_price||0}" ${selectedSvcs.includes(s.service_name)?'checked':''}  onchange="calcOrderTotal()">\n        <span class="text-sm text-ink-400">${s.service_name}\n      ).join('')}</div>\n    </div>\n    <div class=\"grid grid-cols-1 sm:grid-cols-2 gap-3\">\n      <div><label class=\"text-xs font-600 text-ink-300 mb-1 block\">Total (R$)</label><input class=\"input-field\" id=\"of_total\" type=\"number\" step=\"0.01\" value=\"${o?.order_total||''}\"></div>\n      <div><label class=\"text-xs font-600 text-ink-300 mb-1 block\">Status</label><select class=\"input-field\" id=\"of_status\">\n        ${ORDER_STATUSES.map(s => <option ${o?.order_status===s?'selected':''}>${s}).join('')}\n      </select></div>\n      <div><label class=\"text-xs font-600 text-ink-300 mb-1 block\">Operador</label><select class=\"input-field\" id=\"of_operator\">\n        <option value=\"\">Selecione...</option>${collabs.map(c => <option ${o?.order_operator===c.collab_name?'selected':''}>${c.collab_name}).join('')}\n      </select></div>\n      <div><label class=\"text-xs font-600 text-ink-300 mb-1 block\">Data Prevista</label><input class=\"input-field\" id=\"of_due\" type=\"date\" value=\"${o?.order_due_date||''}\"></div>\n    </div>\n    <div><label class=\"text-xs font-600 text-ink-300 mb-1 block\">Observações</label><textarea class=\"input-field\" id=\"of_notes\" rows=\"2\">${o?.order_notes||''}</textarea></div>\n    <div class=\"flex justify-end gap-2 pt-2\">\n      <button type=\"button\" onclick=\"closeModal()\" class=\"btn-secondary rounded-xl px-4 py-2.5 text-sm font-500\">Cancelar</button>\n      <button type=\"submit\" class=\"btn-primary rounded-xl px-5 py-2.5 text-sm font-600\" id=\"ordSaveBtn\">Salvar</button>\n    </div>\n  </form>, 'max-w-xl');\n  // Auto-fill vehicle info\n  document.getElementById('of_client').addEventListener('change', function() {\n    const cl = clients.find(c => c.name === this.value);\n    if(cl) { document.getElementById('of_vehicle').value = cl.vehicle||''; document.getElementById('of_plate').value = cl.plate||''; }\n  });\n}\nfunction calcOrderTotal() {\n  const checks = document.querySelectorAll('.svc-check:checked');\n  let total = 0;\n  checks.forEach(c => total += parseFloat(c.dataset.price||0));\n  const el = document.getElementById('of_total');\n  if(el) el.value = total.toFixed(2);\n}\n\nasync function saveOrder(editId, isQuote) {\n  const btn=document.getElementById('ordSaveBtn');btn.disabled=true;btn.textContent='Salvando...';\n  const checks = [...document.querySelectorAll('.svc-check:checked')].map(c=>c.value);\n  const data = { type:'order', order_client_name:document.getElementById('of_client').value, order_vehicle:document.getElementById('of_vehicle').value, order_plate:document.getElementById('of_plate').value, order_services:checks.join(', '), order_total:document.getElementById('of_total').value, order_status:document.getElementById('of_status').value, order_operator:document.getElementById('of_operator').value, order_due_date:document.getElementById('of_due').value, order_notes:document.getElementById('of_notes').value, order_date:new Date().toISOString(), order_is_quote: isQuote?'true':'false', order_checklist: editId ? (allData.find(d=>d.__backendId===editId)?.order_checklist||'') : '', created_at:new Date().toISOString() };\n  let result;\n  if(editId){const ex=allData.find(d=>d.__backendId===editId);if(ex)result=await window.dataSdk.update({...ex,...data});}\n  else{if(recordCount>=999){toast('Limite atingido','error');btn.disabled=false;btn.textContent='Salvar';return;}result=await window.dataSdk.create(data);}\n  if(result?.isOk){toast(editId?'Atualizado!':'Criado!');closeModal();}else{toast('Erro','error');btn.disabled=false;btn.textContent='Salvar';}\n}\n\nasync function approveQuote(id) {\n  const o = allData.find(d=>d.__backendId===id);\n  if(!o) return;\n  const result = await window.dataSdk.update({...o, order_is_quote:'false', order_status:'Aprovado'});\n  if(result?.isOk) toast('Orçamento aprovado! Convertido em OS.'); else toast('Erro','error');\n}\n\n// ===== CHECKLIST =====\nfunction openChecklist(orderId) {\n  const o = allData.find(d=>d.__backendId===orderId);\n  if(!o) return;\n  let checked = [];\n  try { checked = JSON.parse(o.order_checklist||'[]'); } catch(e) { checked = []; }\n  showModal('Checklist - ' + (o.order_client_name||'OS'), <div class=\"space-y-2\" id=\"checklistItems\">\n    ${CHECKLIST_ITEMS.map((item,i) => <label class="flex items-center gap-3 p-3 border border-surface-200 rounded-xl cursor-pointer hover:bg-surface-50 transition">\n      <input type="checkbox" class="cl-item accent-green-500 w-4 h-4" value="${i}" ${checked.includes(i)?'checked':''}>\n      <span class="text-sm text-ink-400 font-500">${item}\n    ).join('')}\n  </div>\n  <div class=\"flex justify-end gap-2 pt-4\">\n    <button onclick=\"closeModal()\" class=\"btn-secondary rounded-xl px-4 py-2.5 text-sm font-500\">Fechar</button>\n    <button onclick=\"saveChecklist('${orderId}')\" class=\"btn-primary rounded-xl px-5 py-2.5 text-sm font-600\" id=\"clSaveBtn\">Salvar Checklist</button>\n  </div>, 'max-w-sm');\n}\nasync function saveChecklist(orderId) {\n  const btn=document.getElementById('clSaveBtn');btn.disabled=true;btn.textContent='Salvando...';\n  const checked = [...document.querySelectorAll('.cl-item:checked')].map(c=>parseInt(c.value));\n  const o = allData.find(d=>d.__backendId===orderId);\n  if(!o) return;\n  const result = await window.dataSdk.update({...o, order_checklist:JSON.stringify(checked)});\n  if(result?.isOk){toast('Checklist salvo!');closeModal();}else{toast('Erro','error');btn.disabled=false;btn.textContent='Salvar Checklist';}\n}\n\n// ===== SCHEDULE =====\nfunction renderSchedule() {\n  const orders = getByType('order').filter(o => o.order_is_quote!=='true' && o.order_status!=='Cancelado' && o.order_status!=='Concluído');\n  const byDate = {};\n  orders.forEach(o => { const d = o.order_due_date || 'Sem data'; if(!byDate[d]) byDate[d]=[]; byDate[d].push(o); });\n  const sortedDates = Object.keys(byDate).sort();\n  return <div class=\"fade-in\">\n    ${sortedDates.length===0 ? emptyState('calendar','Nenhum agendamento','Crie ordens de serviço com datas') :\n    sortedDates.map(d => <div class="mb-4">\n      <h3 class="font-600 text-ink-400 text-sm mb-2 flex items-center gap-2"><i data-lucide="calendar" style="width:15px;height:15px;" class="text-brand-500"> ${d==='Sem data'?d:formatDate(d+'T00:00:00')}\n      <div class="space-y-2">${byDate[d].map(o => <div class=\"bg-white rounded-xl p-3 border border-surface-200 flex items-center justify-between\">\n        <div>\n          <p class=\"font-500 text-ink-500 text-sm\">${o.order_client_name||'-'} <span class=\"text-ink-100\">•</span> ${o.order_vehicle||'-'}</p>\n          <p class=\"text-xs text-ink-100\">${o.order_services||'-'} ${o.order_operator?'• '+o.order_operator:''}</p>\n        </div>\n        <span class=\"badge ${statusBadge(o.order_status)}\">${o.order_status}</span>\n      </div>).join('')}\n    ).join('')}\n  </div>;\n}\n\n// ===== DELETE =====\nasync function deleteRecord(id, name, label) {\n  showConfirm(Excluir ${name}${label}?, async () => {\n    const rec = allData.find(d => d.__backendId === id);\n    if(!rec) return;\n    const result = await window.dataSdk.delete(rec);\n    if(result?.isOk) toast('Excluído com sucesso!'); else toast('Erro ao excluir','error');\n  });\n}\n\nfunction emptyState(icon, title, sub) {\n  return <div class=\"flex flex-col items-center justify-center py-16 text-center\">\n    <div class=\"w-16 h-16 bg-surface-100 rounded-2xl flex items-center justify-center mb-4\"><i data-lucide=\"${icon}\" style=\"width:28px;height:28px;\" class=\"text-ink-100\"></i></div>\n    <p class=\"font-600 text-ink-300\">${title}</p>\n    <p class=\"text-sm text-ink-100 mt-1\">${sub}</p>\n  </div>;\n}\n\n// ===== OPERADOR =====\nfunction setOpTab(tab) {\n  opTab = tab;\n  document.querySelectorAll('.op-tab-btn').forEach(b => {\n    const isActive = b.dataset.tab === tab;\n    b.className = op-tab-btn flex flex-col items-center gap-1 py-2 px-3 rounded-xl transition ${isActive?'bg-brand-50 text-brand-600':'text-ink-200'};\n  });\n  renderOperador();\n}\nfunction renderOperador() {\n  const c = document.getElementById('operadorContent');\n  const orders = getByType('order').filter(o => o.order_is_quote !== 'true');\n  let html = '';\n  if(opTab==='available') {\n    const avail = orders.filter(o => o.order_status==='Aprovado' && !o.order_operator);\n    html = <div class=\"p-4 space-y-3 fade-in\">\n      <h3 class=\"font-700 text-ink-500\">Ordens Disponíveis</h3>\n      ${avail.length===0?'<p class=\"text-ink-100 text-sm py-8 text-center\">Nenhuma ordem disponível</p>':\n      avail.map(o => opCard(o, true)).join('')}\n    </div>;\n  } else if(opTab==='mine') {\n    const mine = orders.filter(o => o.order_operator === currentOperator && o.order_status !== 'Concluído' && o.order_status !== 'Cancelado');\n    html = <div class=\"p-4 space-y-3 fade-in\">\n      <h3 class=\"font-700 text-ink-500\">Minhas Ordens</h3>\n      ${mine.length===0?'<p class=\"text-ink-100 text-sm py-8 text-center\">Nenhuma ordem atribuída</p>':\n      mine.map(o => opCard(o, false)).join('')}\n    </div>;\n  } else {\n    const mine = orders.filter(o => o.order_operator === currentOperator && (o.order_status==='Em Execução'||o.order_status==='Aprovado'));\n    html = <div class=\"p-4 space-y-3 fade-in\">\n      <h3 class=\"font-700 text-ink-500\">Checklist das Minhas OS</h3>\n      ${mine.length===0?'<p class=\"text-ink-100 text-sm py-8 text-center\">Nenhuma OS ativa</p>':\n      mine.map(o => {\n        let checked=[]; try{checked=JSON.parse(o.order_checklist||'[]');}catch(e){}\n        const pct = Math.round(checked.length/CHECKLIST_ITEMS.length*100);\n        return <div class="bg-white rounded-2xl p-4 border border-surface-200">\n          <div class="flex justify-between items-center mb-3">\n            <p class="font-600 text-ink-500">${o.order_client_name||'-'}<p class="text-xs text-ink-100">${o.order_vehicle||'-'}\n            <span class="text-sm font-700 text-brand-600">${pct}%\n          \n          <div class="h-2 bg-surface-200 rounded-full overflow-hidden mb-3"><div class="h-full bg-brand-400 rounded-full transition-all" style="width:${pct}%">\n          <div class="space-y-2">${CHECKLIST_ITEMS.map((item,i) => <label class=\"flex items-center gap-3 p-2 rounded-lg cursor-pointer hover:bg-surface-50\">\n            <input type=\"checkbox\" class=\"accent-green-500 w-4 h-4\" ${checked.includes(i)?'checked':''} onchange=\"toggleCheckItem('${o.__backendId}',${i},this.checked)\">\n            <span class=\"text-sm ${checked.includes(i)?'text-ink-100 line-through':'text-ink-400'}\">${item}</span>\n          </label>).join('')}\n          \n        ;\n      }).join('')}\n    </div>;\n  }\n  c.innerHTML = html;\n  lucide.createIcons();\n}\n\nfunction opCard(o, showClaim) {\n  return <div class=\"bg-white rounded-2xl p-4 border border-surface-200\">\n    <div class=\"flex justify-between items-start\">\n      <div><p class=\"font-600 text-ink-500\">${o.order_client_name||'-'}</p>\n        <p class=\"text-sm text-ink-200\">${o.order_vehicle||'-'} • ${o.order_plate||'-'}</p>\n        <p class=\"text-xs text-ink-100 mt-1\">${o.order_services||'-'}</p></div>\n      <span class=\"badge ${statusBadge(o.order_status)}\">${o.order_status}</span>\n    </div>\n    <div class=\"flex gap-2 mt-3\">\n      ${showClaim?<button onclick="claimOrder('${o.__backendId}')" class="btn-primary rounded-xl px-4 py-2 text-sm font-600 flex-1">Assumir OS:''}\n      ${!showClaim && o.order_status==='Aprovado'?<button onclick="updateOrderStatus('${o.__backendId}','Em Execução')" class="btn-primary rounded-xl px-4 py-2 text-sm font-600 flex-1">Iniciar:''}\n      ${!showClaim && o.order_status==='Em Execução'?<button onclick="updateOrderStatus('${o.__backendId}','Concluído')" class="bg-blue-500 hover:bg-blue-600 text-white rounded-xl px-4 py-2 text-sm font-600 flex-1 transition">Concluir:''}\n    </div>\n  </div>;\n}\n\nasync function claimOrder(id) {\n  const o = allData.find(d=>d.__backendId===id);\n  if(!o) return;\n  const result = await window.dataSdk.update({...o, order_operator:currentOperator});\n  if(result?.isOk) toast('Ordem assumida!'); else toast('Erro','error');\n}\nasync function updateOrderStatus(id, status) {\n  const o = allData.find(d=>d.__backendId===id);\n  if(!o) return;\n  const result = await window.dataSdk.update({...o, order_status:status});\n  if(result?.isOk) toast(Status: ${status}); else toast('Erro','error');\n}\nasync function toggleCheckItem(orderId, idx, checked) {\n  const o = allData.find(d=>d.__backendId===orderId);\n  if(!o) return;\n  let items=[]; try{items=JSON.parse(o.order_checklist||'[]');}catch(e){}\n  if(checked && !items.includes(idx)) items.push(idx);\n  if(!checked) items = items.filter(i=>i!==idx);\n  await window.dataSdk.update({...o, order_checklist:JSON.stringify(items)});\n}\n\n// ===== TV =====\nlet tvInterval;\nfunction startTVClock() {\n  if(tvInterval) clearInterval(tvInterval);\n  updateTVClock();\n  tvInterval = setInterval(updateTVClock, 1000);\n}\nfunction updateTVClock() {\n  const now = new Date();\n  const timeEl = document.getElementById('tvTime');\n  const dateEl = document.getElementById('tvDate');\n  if(timeEl) timeEl.textContent = now.toLocaleTimeString('pt-BR', {hour:'2-digit',minute:'2-digit',second:'2-digit'});\n  if(dateEl) dateEl.textContent = now.toLocaleDateString('pt-BR', {weekday:'long',day:'numeric',month:'long'});\n}\nfunction renderTV() {\n  const orders = getByType('order').filter(o => o.order_is_quote!=='true' && o.order_status!=='Cancelado');\n  const inExec = orders.filter(o => o.order_status==='Em Execução');\n  const approved = orders.filter(o => o.order_status==='Aprovado');\n  const done = orders.filter(o => o.order_status==='Concluído');\n\n  const statsEl = document.getElementById('tvStats');\n  statsEl.innerHTML = [\n    { label:'Em Execução', value:inExec.length, icon:'zap', bg:'bg-amber-500/20', text:'text-amber-400' },\n    { label:'Aguardando', value:approved.length, icon:'clock', bg:'bg-blue-500/20', text:'text-blue-400' },\n    { label:'Concluídas', value:done.length, icon:'check-circle', bg:'bg-brand-500/20', text:'text-brand-400' },\n    { label:'Total OS', value:orders.length, icon:'clipboard-list', bg:'bg-purple-500/20', text:'text-purple-400' },\n  ].map(s => <div class=\"${s.bg} rounded-2xl p-4 border border-white/5\">\n    <div class=\"flex items-center gap-2 mb-2\"><i data-lucide=\"${s.icon}\" style=\"width:18px;height:18px;\" class=\"${s.text}\"></i><span class=\"text-sm text-slate-400\">${s.label}</span></div>\n    <p class=\"text-3xl font-800 text-white\">${s.value}</p>\n  </div>).join('');\n\n  const active = orders.filter(o => o.order_status==='Em Execução' || o.order_status==='Aprovado');\n  const tableEl = document.getElementById('tvTable');\n  tableEl.innerHTML = active.length === 0 ? <div class=\"flex items-center justify-center h-full\"><p class=\"text-slate-500 text-lg\">Nenhuma ordem ativa no momento</p></div> :\n  <table class=\"w-full\">\n    <thead><tr class=\"text-slate-400 text-sm text-left border-b border-white/10\">\n      <th class=\"py-3 px-4 font-500\">Status</th>\n      <th class=\"py-3 px-4 font-500\">Cliente</th>\n      <th class=\"py-3 px-4 font-500\">Veículo</th>\n      <th class=\"py-3 px-4 font-500\">Placa</th>\n      <th class=\"py-3 px-4 font-500\">Serviços</th>\n      <th class=\"py-3 px-4 font-500\">Operador</th>\n      <th class=\"py-3 px-4 font-500\">Previsão</th>\n    </tr></thead>\n    <tbody>${active.map(o => {\n      const statusColor = o.order_status==='Em Execução'?'text-amber-400 bg-amber-500/20':'text-blue-400 bg-blue-500/20';\n      return <tr class="border-b border-white/5 text-white">\n        <td class="py-4 px-4"><span class="px-3 py-1 rounded-full text-xs font-600 ${statusColor}">${o.order_status}\n        <td class="py-4 px-4 font-500">${o.order_client_name||'-'}\n        <td class="py-4 px-4 text-slate-300">${o.order_vehicle||'-'}\n        <td class="py-4 px-4"><span class="bg-white/10 px-2 py-1 rounded-lg text-xs font-600">${o.order_plate||'-'}\n        <td class="py-4 px-4 text-slate-300 text-sm">${o.order_services||'-'}\n        <td class="py-4 px-4"><span class="text-brand-400 font-500">${o.order_operator||'-'}\n        <td class="py-4 px-4 text-slate-400 text-sm">${o.order_due_date ? formatDate(o.order_due_date+'T00:00:00') : '-'}\n      ;\n    }).join('')}</tbody>\n  </table>;\n  lucide.createIcons();\n}\n\n// ===== DATA SDK =====\nconst dataHandler = {\n  onDataChanged(data) {\n    allData = data;\n    recordCount = data.length;\n    // Update active view\n    if(currentRole === 'gestor') renderGestorContent();\n    else if(currentRole === 'operador') renderOperador();\n    else if(currentRole === 'tv') renderTV();\n  }\n};\n\n// ===== ELEMENT SDK =====\nfunction applyConfig(config) {\n  const title = config.app_title || defaultConfig.app_title;\n  const welcome = config.welcome_text || defaultConfig.welcome_text;\n  const font = config.font_family || defaultConfig.font_family;\n  const baseSize = config.font_size || defaultConfig.font_size;\n  const primaryColor = config.primary_color || defaultConfig.primary_color;\n  const surfaceColor = config.surface_color || defaultConfig.surface_color;\n  const textColor = config.text_color || defaultConfig.text_color;\n  const actionColor = config.action_color || defaultConfig.action_color;\n  const secActionColor = config.secondary_action_color || defaultConfig.secondary_action_color;\n\n  // Apply titles\n  const ids = ['loginTitle','sidebarTitle','opTitle','tvTitle'];\n  ids.forEach(id => { const el = document.getElementById(id); if(el) el.textContent = title; });\n  const welcomeEl = document.getElementById('loginWelcome');\n  if(welcomeEl) welcomeEl.textContent = welcome;\n\n  // Apply font\n  document.body.style.fontFamily = ${font}, Plus Jakarta Sans, sans-serif;\n  document.body.style.fontSize = ${baseSize}px;\n\n  // Apply colors via CSS custom props used in inline styles\n  document.documentElement.style.setProperty('--brand-primary', primaryColor);\n  // Update btn-primary and brand elements\n  document.querySelectorAll('.btn-primary').forEach(el => { el.style.backgroundColor = actionColor; });\n}\n\n(async () => {\n  // Init Element SDK\n  if(window.elementSdk) {\n    window.elementSdk.init({\n      defaultConfig,\n      onConfigChange: async (config) => { applyConfig(config); },\n      mapToCapabilities: (config) => ({\n        recolorables: [\n          { get:()=>config.primary_color||defaultConfig.primary_color, set:(v)=>{config.primary_color=v; window.elementSdk.setConfig({primary_color:v});} },\n          { get:()=>config.surface_color||defaultConfig.surface_color, set:(v)=>{config.surface_color=v; window.elementSdk.setConfig({surface_color:v});} },\n          { get:()=>config.text_color||defaultConfig.text_color, set:(v)=>{config.text_color=v; window.elementSdk.setConfig({text_color:v});} },\n          { get:()=>config.action_color||defaultConfig.action_color, set:(v)=>{config.action_color=v; window.elementSdk.setConfig({action_color:v});} },\n          { get:()=>config.secondary_action_color||defaultConfig.secondary_action_color, set:(v)=>{config.secondary_action_color=v; window.elementSdk.setConfig({secondary_action_color:v});} },\n        ],\n        borderables: [],\n        fontEditable: { get:()=>config.font_family||defaultConfig.font_family, set:(v)=>{config.font_family=v; window.elementSdk.setConfig({font_family:v});} },\n        fontSizeable: { get:()=>config.font_size||defaultConfig.font_size, set:(v)=>{config.font_size=v; window.elementSdk.setConfig({font_size:v});} },\n      }),\n      mapToEditPanelValues: (config) => new Map([\n        ['app_title', config.app_title || defaultConfig.app_title],\n        ['company_name', config.company_name || defaultConfig.company_name],\n        ['welcome_text', config.welcome_text || defaultConfig.welcome_text],\n      ])\n    });\n  }\n\n  // Init Data SDK\n  if(window.dataSdk) {\n    const initResult = await window.dataSdk.init(dataHandler);\n    if(!initResult.isOk) console.error('Data SDK init failed');\n  }\n\n  // Set current date\n  document.getElementById('currentDate').textContent = new Date().toLocaleDateString('pt-BR', {weekday:'long',day:'numeric',month:'long'});\n\n  lucide.createIcons();\n})();\n\n\n"}}
</tool_use>